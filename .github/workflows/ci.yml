name: solution CI

on:
  push:
    branches: [main, "**"]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: revenue
          POSTGRES_PASSWORD: revenue
          POSTGRES_DB: hotel_revenue
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U revenue -d hotel_revenue"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      DATABASE_URL: postgresql://revenue:revenue@localhost:5432/hotel_revenue
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Apply schema and views
        run: |
          uv run python - <<'PY'
          import psycopg, pathlib
          url = "postgresql://revenue:revenue@localhost:5432/hotel_revenue"
          schema = pathlib.Path("schema.sql").read_text()
          overrides = pathlib.Path("sql/schema_overrides.sql").read_text()
          views = pathlib.Path("sql/views.sql").read_text()
          with psycopg.connect(url) as c:
              with c.cursor() as cur:
                  cur.execute(schema); cur.execute(overrides); cur.execute(views)
              c.commit()
          print("schema + overrides + views applied")
          PY

      - name: Lint
        run: uv run ruff check .

      - name: Test
        run: uv run pytest -q
