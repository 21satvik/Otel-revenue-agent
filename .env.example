# Copy to .env (gitignored) and fill in. Never commit real secrets.

# Postgres connection. Local docker-compose / cluster default:
DATABASE_URL=postgresql://hackathon:hackathon@localhost:5432/hotel_hackathon

# Anthropic API key for the agent (set in the deploy environment, never in git).
ANTHROPIC_API_KEY=sk-ant-...

# Model id (provider:model). Swappable; Claude by default.
MODEL_ID=anthropic:claude-sonnet-4-6

# HTTP basic auth for the deployed app. Send these via private intake, not the repo.
BASIC_AUTH_USER=admin
BASIC_AUTH_PASS=change-me

# Tool transport: "direct" (in-process, the default, used by tests and local runs) or
# "mcp" (the agent loads the five tools from the MCP server). The deployment uses "mcp".
RM_TOOL_TRANSPORT=direct

# MCP server URL, only when RM_TOOL_TRANSPORT=mcp and the server runs over streamable-http.
# Unset, the agent falls back to a local stdio subprocess. The deployment points this at
# the otel-mcp service.
MCP_SERVER_URL=http://127.0.0.1:9000/mcp
